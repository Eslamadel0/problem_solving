/*#include <stdio.h>
#include <stdlib.h>
int main (void){

	setbuf(stdout,NULL);
    int f,i;
    int n;
    f=1;
    printf("Enter any number to calculate the factorial : ");
    scanf("%d",&n);

    for (i=1; i<=n; i++)
    {
    	f*=i;
    }
    printf("the factorial is = %d    when the number = %d ",f,n);
    return 0;
}*/
